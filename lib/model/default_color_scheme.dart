import 'package:flutter/material.dart';

const primaryColor = Color(0xFF63A002);

final defaultLightColorScheme = ColorScheme.fromSeed(seedColor: primaryColor);
final defaultDarkColorScheme =
    ColorScheme.fromSeed(seedColor: primaryColor, brightness: Brightness.dark);
